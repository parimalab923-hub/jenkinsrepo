pipeline {
    agent any {
        stages {
            stage("welcome to dvs") {
                steps {
                    script {
                        var1=10
                        var2="dvs"
                        println "myvar1 value is ${var1} and myvar2 value is ${var2}"
                    }
                }
            }
        }
    }
}

